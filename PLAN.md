# План доработок (батч) — удалить после выполнения

## 1. OWUI: code_interpreter для DB-моделей — отключить и спрятать
- [ ] Изучить: где OWUI инъектирует секцию «Code Interpreter» в промпт (источник путаницы CLI-агентов);
- [ ] Изучить: настраивается ли выключение по умолчанию через админ-панель/.env (DEFAULT_MODEL_METADATA? ENABLE_CODE_INTERPRETER?), иначе менять код;
- [ ] Выключить по умолчанию + убрать кнопку включения у пользователей;
- [ ] Массово выключить code_interpreter в существующих 41 DB-карточках (скриптом через API).

## 2. OWUI: webSearch/capabilities мета — отключить
- [ ] Проверить, что OWUI шлёт моделям web_tools/webSearch мету; выключить (гуглинг делает шлюз: gpt-канал — web_search tool, CLI-канал — терминал).

## 3. Шлюз: агент должен сразу знать, как искать в интернете
- [ ] agent_prompt.txt: добавить секцию Environment (нет поисковых MCP-инструментов; интернет — через терминал: python urllib / RSS-ленты), чтобы «нет web tool»-блуждания исчезли.

## 4. Шлюз: генерировать КОНФИГ CLI богатым (как у провайдера) — ПРИОРИТЕТ
Найденная точная причина деградации: наш entrypoint генерирует бедный конфиг
(контекст хардкод 131072 вместо реального, нет reasoning_efforts/дефолтов усилия,
нет имён), а локальный CLI у пользователя работает с богатым конфигом от каталога
провайдера (glm: default effort max, grok: xhigh, контекст 1М + [tools]/[memory]).
Отсюда: у нас агент на низком усилии — блуждания, галлюцинации в промежуточных ходах,
3 минуты; локально — 22 секунды чисто.
- [ ] Entry point: генерить конфиг из каталога провайдера (context_window, display_name,
      supports_reasoning_effort, reasoning_efforts + дефолт усилия по семейству как в локальном);
- [ ] Убрать хардкод context_window = 131072;
- [ ] Проверить A/B: богатый конфиг + агентский промпт = поведение как локальное.

## Сделано ранее (в этой же теме)
- [x] Уровни размышлений из провайдера, наследование в DB-карточки, селектор >1 уровня;
- [x] gpt-канал: web_search tool автоматически (web_search: true);
- [x] CLI-канал: пермодельная личность без компании, рассуждения/ответ раздельно;
- [x] Иконки по классам, красивые имена из display_name.
