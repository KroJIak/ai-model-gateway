services:
  model-bridge:
    ports:
      - "127.0.0.1:${BRIDGE_EXTERNAL_PORT-8081}:8080"
  gpt-api:
    ports:
      - "127.0.0.1:${GPT_API_EXTERNAL_PORT-18317}:8317"
  grok-cli-api:
    ports:
      - "127.0.0.1:${GROK_API_EXTERNAL_PORT-18090}:8090"
